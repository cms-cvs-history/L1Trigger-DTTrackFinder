process DTTF = {
        source = PoolSource{
                untracked int32 maxEvents = -1
                untracked vstring fileNames = {'file:DTprimitives.root'}
        }

	include "L1TriggerConfig/DTTrackFinder/data/L1DTTrackFinderConfig.cff"

	include "L1Trigger/DTTrackFinder/data/dttfDigis.cfi"

	module out = PoolOutputModule{
        	untracked string fileName ="runDTTF.root"
	}

	path p = {DTTrackFinder}
	endpath pp = {out}
}
